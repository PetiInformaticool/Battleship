#pragma once
#include "game.h"
#include <bits/stdc++.h>

void drawBoard(board b[], int cnt[], Player2 Move, int turn, int type, int killed[][10], std::string winnerwinnerchickendinner);

void nextmove();

void display();

int mainLoop (int argc, char** argv);
